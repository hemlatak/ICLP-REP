
select DISTINCT ACCOUNT_NUMBER,CUSTOMER_ID from NCB_CMS_CUSTOMER_POINTS_REDEEMED R
WHERE 
EXISTS (SELECT 1 FROM temp_1110_mem_mulple_card T 
WHERE T.ACCOUNT_NUMBER = R.ACCOUNT_NUMBER AND T.CUSTOMER_ID = R.CUSTOMER_ID AND POINTS_REDEEMED>0)